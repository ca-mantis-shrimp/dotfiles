[Distribution]
Distribution=arch

[Output]
Format=directory
OutputDirectory=mkosi.output

[Content]
Packages=base
         git
         curl
         bash
         neovim
         chezmoi
         nodejs
         npm

[Build]
WithNetwork=yes

[Config]
PassEnvironment=CLAUDE_CODE_OAUTH_TOKEN
