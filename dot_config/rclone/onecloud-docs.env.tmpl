SRC_DIR="$HOME/Documents"
DEST_REMOTE="OneDrive:"

# Set to false after verifying first dry run.
RCLONE_DRY_RUN=false

# Optional tunables
# RCLONE_TRANSFERS=4
# RCLONE_CHECKERS=8
# RCLONE_RETRIES=5
# RCLONE_LOW_LEVEL_RETRIES=10
# RCLONE_TIMEOUT=1m
# RCLONE_LOG_LEVEL=INFO

# Optional: one exclude pattern per line.
# RCLONE_EXCLUDE_FROM="$HOME/.config/rclone/onecloud-docs-exclude.txt"

# Optional: appended at the end as raw args.
# RCLONE_EXTRA_ARGS="--bwlimit 8M"
