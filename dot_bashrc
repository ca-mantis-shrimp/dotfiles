#!/bin/bash

eval "$(uwsm app starship init bash)"

# Set up fzf key bindings and fuzzy completion
eval "$(uwsm app -- fzf --bash)"
# properly setup environment variables for lua
eval $(uwsm app -- luarocks path --bin)
