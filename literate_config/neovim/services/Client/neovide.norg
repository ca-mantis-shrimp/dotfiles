@document.meta
title: Neovide Client Systemd Service
description: Systemd service to connect to the domain server and get it working using a shared domain UNIX socket
authors: primary_desktop
categories: Neovim GUI
tangle: ../../../../dot_config/systemd/remove_user/neovide-client.service
created: 2025-08-31T10:45:54-0800
updated: 2025-09-16T22:03:51-0800
version: 1.1.1
@end

The GUI client service now depends on the socket unit rather than the service directly, allowing for proper on-demand activation.

#tangle 
@code ini
[Unit]
Description=Neovide GUI Instance (On-Demand Neovim Connection)
Documentation=https://neovide.dev
After=neovim-server-proxy.socket

[Service]
Type=exec
ExecStart=/usr/bin/neovide --server /run/user/1000/nvim/server.sock

[Install]
WantedBy=default.target
@end

Changes from previous version:
- `After=neovim-server.socket` instead of `Requires=neovim-server.service`
- Added restart logic for robustness
- The socket activation will start the server automatically when neovide connects
